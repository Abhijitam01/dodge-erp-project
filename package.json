{
  "name": "dodge-ai-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --names server,client --prefix-colors blue,green \"pnpm -C server dev\" \"pnpm -C client dev\"",
    "build": "pnpm -C server build && pnpm -C client build",
    "start": "concurrently --names server,client --prefix-colors blue,green \"pnpm -C server start\" \"pnpm -C client preview\"",
    "ingest": "pnpm -C server ingest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.30.3",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@types/cors": "^2.8.19",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
